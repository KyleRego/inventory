<p><%= notice %></p>

<%= render @item %>

<div>
  <%= link_to "Edit this item", edit_item_path(@item) %>

  <%= button_to "Destroy this item", @item, method: :delete, class: "danger-button",
        form: { data: { turbo_confirm: "Are you sure you want to delete #{@item.name}?"} } %>
</div>
