<h1 class="fs-8 mb-4">
  Editing item: <a class="text-link" href="<%= item_path(@item) %>">
                  <%= @item.name %>
                </a>
          <% if @location %>
            at <a class="text-link" href="<%= location_path(@location) %>">
                  <%= @location.name %>
                </a>
          <% end %>
</h1>

<%= render "form", item: @item %>
