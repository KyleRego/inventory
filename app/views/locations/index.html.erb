<p><%= notice %></p>

<h1 class="fs-8 text-align-center border-bottom-4 border-style-groove">Locations</h1>

<div id="locations" class="text-align-center mt-2 flex flex-row flex-wrap">
  <% @locations.each do |location| %>

    <div id="<%= dom_id location %>" class="mr-2 mb-2 border-around-2 border-style-groove">
      <div class="p-2">
        <div class="fs-5">
          <%= location.name %>
        </div>

        <div class="text-align-center">
          <%= location.description %>
        </div>
      </div>
    </div>

  <% end %>
</div>

<%= link_to "New location", new_location_path %>
