<!DOCTYPE html>
<html>
  <head>
    <title>Inventory</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="font-gill-sans">
    <header class="bg-light-gray header-box-shadow h-12 p-4 mb-4 flex justify-between align-items-center">
      <div class="inline flex align-items-center">
        <span class="mr-2">
          <%= render partial: "layouts/heroicons/cube",
                locals: { css_class: "inline w-8 h-8" } %>
        </span>

        <span class="fs-5">Inventory</span>
      </div>

      <nav class="flex-1 sm-hidden">
        <ul class="flex justify-end">
          <li class="inline mr-4"><%= link_to "New location", new_location_path, class: "text-link" %></li>
          <li class="inline mr-4"><%= link_to "New item", new_item_path, class: "text-link" %></li>
          <li class="inline mr-4"><%= link_to "Locations", locations_path, class: "text-link" %></li>
          <li class="inline mr-4"><%= link_to "Items", items_path, class: "text-link" %></li>
        </ul>
      </nav>

      <div class="inline mr-2 sm-show" data-controller="hamburger">
        <button type="button" data-hamburger-target="showButton" title="Show navigation menu">
          <%= render partial: "layouts/heroicons/hamburger",
                      locals: { css_class: "inline w-8 h-8" } %>
        </button>

        <div class="position-fixed invisible top-4 right-4 bg-dark-white p-2" data-hamburger-target="menu">
          <div class="flex justify-end">
            <button type="button" data-hamburger-target="hideButton" title="Hide navigation menu">
              <%= render partial: "layouts/heroicons/x",
                          locals: { css_class: "inline w-8 h-8" } %>
            </button>
          </div>
          <ul>
            <li><%= link_to "New location", new_location_path, class: "text-link" %></li>
            <li><%= link_to "New item", new_item_path, class: "text-link" %></li>
            <li><%= link_to "Locations", locations_path, class: "text-link" %></li>
            <li><%= link_to "Items", items_path, class: "text-link" %></li>
          </ul>
        </div>
      </div>
    </header>

    <main class="ml-4 mr-4 mb-4 pl-1 pr-1 pb-1">
      <%= yield %>
    </main>
  </body>
</html>
